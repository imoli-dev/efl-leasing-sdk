---
title: StatusBlueMedia
description: Enum reprezentujący status procesu weryfikacji tożsamości BlueMedia.
---

## Przestrzeń nazw i przeznaczenie

`Imoli\EflLeasingSdk\Model\Verification\StatusBlueMedia` to enum opisujący bieżący status procesu weryfikacji BlueMedia.
Uzupełnia [`ResultBlueMedia`](41.result-blue-media.md) w [`BlueMediaProcessStateResponse`](40.blue-media-process-state-response.md).

## Definicja klasy

- **Przestrzeń nazw**: `Imoli\EflLeasingSdk\Model\Verification`
- **Typ**: enum `StatusBlueMedia`

```php
enum StatusBlueMedia: string
{
    case Ok = 'OK';
    case Error = 'ERROR';
    case Pending = 'PENDING';
}
```

## Wartości

| Wartość   | Wartość tekstowa | Opis                                                     |
|-----------|--------------|----------------------------------------------------------|
| `Ok`      | `'OK'`       | Proces weryfikacji jest w stanie sukcesu lub stabilnym.  |
| `Error`   | `'ERROR'`    | Wystąpił błąd.                                           |
| `Pending` | `'PENDING'`  | Weryfikacja jest w toku.                                 |

## Użycie w SDK

- Zwracane przez `BlueMediaProcessStateResponse::getStatus()`.
- Powinien być sprawdzany przez logikę aplikacji wraz z wynikiem, aby zdecydować, czy czekać, spróbować ponownie, czy kontynuować.
