---
title: AssetToCalculationBuilder
description: Fluent builder do konstruowania koszyków AssetToCalculation dla podstawowych kalkulacji ofert.
---

## Przestrzeń nazw i przeznaczenie

`Imoli\EflLeasingSdk\Builder\AssetToCalculationBuilder` to pomocnik typu fluent do budowania instancji `AssetToCalculation`.
Jest on zazwyczaj używany przed wywołaniem `EflClient::calculateBasicOffer()` lub niskopoziomowego `CalculationApiClient::calculateBasicOffer()`.

## Definicja klasy

- **Przestrzeń nazw**: `Imoli\EflLeasingSdk\Builder`
- **Klasa**: final `AssetToCalculationBuilder`
- **Buduje**: `Imoli\EflLeasingSdk\Model\Calculation\AssetToCalculation`

```php
namespace Imoli\EflLeasingSdk\Builder;

use Imoli\EflLeasingSdk\Model\Calculation\AssetToCalculation;
use Imoli\EflLeasingSdk\Model\Calculation\OfferItem;

final class AssetToCalculationBuilder
{
    public function withTransactionId(string $transactionId): self;

    /** @return $this */
    public function addOfferItem(OfferItem $item): self;

    /**
     * @param OfferItem[] $items
     * @return $this
     */
    public function withOfferItems(array $items): self;

    public function withReturnToBasketUrl(?string $returnToBasketUrl): self;

    public function withBasketCalculation(?bool $basketCalculation): self;

    public function build(): AssetToCalculation;

    public static function create(string $transactionId): self;
}
```

## Fluent API

- **withTransactionId(string $transactionId): self**  
  Ustawia identyfikator transakcji dla żądania kalkulacji.

- **addOfferItem(OfferItem $item): self**  
  Dodaje pojedynczy `OfferItem` do koszyka.

- **withOfferItems(OfferItem[] $items): self**  
  Zastępuje bieżącą listę przedmiotów oferty dostarczoną tablicą.

- **withReturnToBasketUrl(?string $returnToBasketUrl): self**  
  Opcjonalnie ustawia `returnToBasketUrl` używany do przekierowania użytkownika z powrotem do koszyka.

- **withBasketCalculation(?bool $basketCalculation): self**  
  Kontroluje, czy kalkulacja jest wykonywana dla całego koszyka.

- **build(): AssetToCalculation**  
  - Waliduje, czy **`transactionId`** jest ustawiony i czy obecny jest co najmniej **jeden `OfferItem`**.  
  - Rzuca `\LogicException`, jeśli brakuje wymaganych danych.  
  - Zwraca w pełni zainicjalizowaną instancję `AssetToCalculation`.

- **static create(string $transactionId): self**  
  Wygodna fabryka, która zwraca nowy builder z już ustawionym `transactionId`.

## Przykłady użycia

### Podstawowy koszyk do kalkulacji

```php
use Imoli\EflLeasingSdk\Builder\AssetToCalculationBuilder;
use Imoli\EflLeasingSdk\Builder\OfferItemBuilder;
use Imoli\EflLeasingSdk\Builder\ItemDetailBuilder;

$itemDetail = ItemDetailBuilder::create('asset-code', 'Laptop X')
    ->build();

$offerItem = OfferItemBuilder::create(1, 'asset-guid-1', 23.0, [$itemDetail])
    ->build();

$basket = AssetToCalculationBuilder::create('tx-1')
    ->addOfferItem($offerItem)
    ->withReturnToBasketUrl('https://shop.example.com/basket')
    ->withBasketCalculation(true)
    ->build();
```

### Użycie z EflClient

```php
use Imoli\EflLeasingSdk\EflClient;

// $eflClient to instancja EflClient skonfigurowana dla piaskownicy lub produkcji
$result = $eflClient->calculateBasicOffer($basket, $bearerToken);
```
