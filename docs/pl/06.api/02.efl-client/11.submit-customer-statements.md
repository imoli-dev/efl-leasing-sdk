---
title: submitCustomerStatements
description: Przesyła dodatkowe oświadczenia klienta dla danej transakcji.
---

`submitCustomerStatements()` wysyła dodatkowe oświadczenia/zgody dla transakcji.
Opakowuje `CustomerApiClient::postCustomerStatements()`.

Wszystkie metody w [`EflClient`](01.overview.md) mogą zgłaszać:

- [`ApiException`](../06.errors.md#apiexception-and-problemdetails) – gdy API EFL zwróci odpowiedź z błędem.
- [`HttpException`](../06.errors.md#httpexception) – gdy transport HTTP zawiedzie.

## Sygnatura metody

```php
public function submitCustomerStatements(
    string $transactionId,
    array $statements,
    string $bearerToken
): void
```

## Parametry

| Nazwa           | Typ                                                                                             | Wymagany | Opis                                            |
| -------------- | ------------------------------------------------------------------------------------------------ | -------- | ----------------------------------------------- |
| `transactionId`| `string`                                                                                        | Tak      | Identyfikator transakcji.                       |
| `statements`   | `CustomerDataStatement[]` ([`CustomerDataStatement`](../04.models/21.customer-data-statement.md)) | Tak      | Lista oświadczeń/zgód do przesłania.            |
| `bearerToken`  | `string`                                                                                        | Tak      | Token Bearer.                                   |

## Wartość zwracana

- **Typ**: `void` – niepowodzenia są zgłaszane poprzez wyjątki.
