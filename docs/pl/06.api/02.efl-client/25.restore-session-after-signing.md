---
title: restoreSessionAfterSigning
description: Przywraca sesję klienta po podpisaniu dla danej transakcji.
---

`restoreSessionAfterSigning()` przywraca sesję klienta po kroku podpisania dla transakcji.
Opakowuje `RestorationApiClient::restoreSessionAfterSigning()` i mapuje odpowiedź na `AuthenticationRestorationResult`.

Wszystkie metody w [`EflClient`](01.overview.md) mogą zgłaszać:

- [`ApiException`](../06.errors.md#apiexception-and-problemdetails) – gdy API EFL zwróci odpowiedź z błędem.
- [`HttpException`](../06.errors.md#httpexception) – gdy transport HTTP zawiedzie.

## Sygnatura metody

```php
public function restoreSessionAfterSigning(
    string $transactionId
): AuthenticationRestorationResult
```

## Parametry

| Nazwa           | Typ      | Wymagany | Opis                             |
| -------------- | -------- | -------- | ------------------------------- |
| `transactionId`| `string` | Tak      | Identyfikator transakcji.       |

## Wartość zwracana

- **Typ**: [`AuthenticationRestorationResult`](../04.models/35.authentication-restoration-result.md) – wynik przywracania.
